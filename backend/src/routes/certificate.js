const express = require('express');
const router = express.Router();

// Certificate routes are handled in employee.js and hr.js
// This file is kept for future certificate-specific routes

module.exports = router;
